{
  "name": "faucet-homework",
  "version": "1.0.0",
  "description": "Solidity Faucet with Owned/Pausable, rate limiting and whitelist. Raw solc + ethers.js (no framework).",
  "private": true,
  "scripts": {
    "compile": "solcjs --bin --abi --optimize -o out contracts/Faucet.sol && node scripts/rename-artifacts.js",
    "deploy": "node scripts/deploy-ethers.js",
    "interact": "node scripts/whitelist-and-test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "solc": "^0.8.26"
  }
}
